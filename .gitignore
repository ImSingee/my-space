node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
test-results
playwright-report
__screenshots__
.vitest
todos.json

# BYOD platform runtime data (app sources, build artifacts, temp)
/workspace
# pi agent credential file (we inject keys explicitly instead)
auth.json
.pi
