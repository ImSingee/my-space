node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.env.*.tmp
# Legacy Agent credential files remain ignored across the `.env` migration.
.secrets
.secrets.*.tmp
/workspace/
.nitro
.tanstack
.wrangler
.output
.vinxi
test-results
playwright-report
.playwright-cli/
.pnpm-store/
__screenshots__
.vitest
todos.json

# Local-only big-feature progress tracker (not part of the repo)
/PROGRESS.md

# pi agent credential file (we inject keys explicitly instead)
auth.json
.pi
.vitest-attachments
