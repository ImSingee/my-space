node_modules
.DS_Store
dist
dist-runner
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
test-results
playwright-report
__screenshots__
.vitest
todos.json

# Local-only big-feature progress tracker (not part of the repo)
/PROGRESS.md

# pi agent credential file (we inject keys explicitly instead)
auth.json
.pi
.vitest-attachments
