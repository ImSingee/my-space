/**
 * Temporary product capability switches shared by the Platform UI and Agent.
 * Set this back to true to restore the existing Workflow surfaces.
 */
export const WORKFLOWS_ENABLED: boolean = false;
